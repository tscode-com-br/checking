# Checklist de prompts para executar o plano de `temp_014.md`

Este arquivo transforma o plano de `docs/temp_014.md` em uma to-do list operacional. Cada item abaixo ja esta redigido como um prompt completo, pronto para ser entregue a um agente de IA para implementacao cuidadosa, validacao e fechamento.

## Checklist principal

- [ ] Prompt 1 - Fechar o diagnostico da dropdown vazia do modal `IA SETTINGS`

  Voce e um agente de IA trabalhando no repositorio `checkcheck`. Seu objetivo nesta tarefa e identificar, com evidencia objetiva, por que a dropdown de projetos do modal `IA SETTINGS` do dashboard `transport` aparece vazia em runtime. Trabalhe primeiro em diagnostico, nao em correcao ampla. Comece pelos arquivos `sistema/app/static/transport/app.js`, `sistema/app/static/transport/index.html`, `sistema/app/static/transport/i18n.js`, `sistema/app/routers/transport.py`, `sistema/app/schemas.py` e `tests/transport_page_date.test.js`. Verifique como o frontend le `state.dashboard.projects`, como normaliza a lista em `normalizeTransportAiSettingsProjectRows()`, como faz fallback para `GET ../api/transport/projects` em `loadTransportAiSettingsProjectCatalog()`, como o backend monta `TransportDashboardResponse.projects` e como o endpoint dedicado `/api/transport/projects` responde na mesma sessao autenticada. Capture e compare os payloads reais de `GET /api/transport/dashboard` e `GET /api/transport/projects`, confirmando quantidade de itens, shape de cada projeto, presenca de `id` e `name`, diferencas de auth, ordem, filtros ou prefixo de URL. Confirme tambem se o HTML servido em runtime contem `data-ai-settings-project` e se os assets `styles.css`, `i18n.js` e `app.js` estao na versao esperada, sem cliente servindo bundle antigo em cache. Se necessario, adicione instrumentacao local temporaria e reversivel apenas para comprovar a transicao de `state.aiSettingsProjects`, `state.aiSettingsSelectedProjectId` e `state.aiSettingsDraft.projectId` na abertura do modal. Nao faca alteracoes funcionais amplas antes de conseguir nomear a causa primaria e, se houver, a causa secundaria. Ao final, entregue uma conclusao objetiva apontando qual e a fonte autoritativa correta do catalogo de projetos, qual e o ponto exato em que a lista se perde e qual e a menor mudanca plausivel para corrigir o problema sem regressao.

- [ ] Prompt 2 - Implementar a carga autoritativa e resiliente da dropdown de projetos

  Voce e um agente de IA trabalhando no repositorio `checkcheck`. Com base no diagnostico ja fechado para a dropdown vazia do modal `IA SETTINGS`, implemente a correcao de frontend de forma robusta, previsivel e auditavel. O objetivo e fazer com que a dropdown de projetos sempre use `GET /api/transport/projects` como fonte autoritativa, mantendo `state.dashboard.projects` e cache local apenas como bootstrap visual temporario. Trabalhe principalmente em `sistema/app/static/transport/app.js` e, se necessario, em `sistema/app/static/transport/index.html` e `sistema/app/static/transport/i18n.js`. Reestruture o fluxo de carregamento do catalogo do modal para suportar estados explicitos como `idle`, `loading`, `ready`, `empty` e `error`, preservando o `selectedProjectId` quando o projeto ainda existir na resposta autoritativa e limpando hint, draft e selecao apenas quando o projeto tiver realmente deixado de existir. Nao trate `dashboard.projects` como resposta final silenciosa. Ao abrir o modal, o frontend pode mostrar dados locais como estado inicial, mas deve sempre disparar o refresh autoritativo para `GET /api/transport/projects` antes de considerar o catalogo pronto para gravacao. Garanta que `applyTransportAiSettingsProjects()` ou funcao equivalente seja chamada a partir de uma reconciliacao unica e coerente, para evitar listas parcialmente normalizadas em caminhos diferentes. Se o endpoint falhar e nao houver projetos validos, mostre um estado vazio controlado e diagnostico claro; se houver projetos locais validos e a resposta autoritativa falhar, mantenha a interface consistente, mas evite que o usuario grave com estado obsoleto. Preserve o comportamento existente do restante do dashboard e nao introduza mudancas de layout fora do modal. Depois da primeira edicao substantiva, valide com o recorte mais estreito possivel e so continue se a validacao confirmar que a dropdown deixou de ficar vazia quando existem projetos validos.

- [ ] Prompt 3 - Endurecer o fluxo de selecao, bloqueio de `Save` e mensagens de erro do modal

  Voce e um agente de IA trabalhando no repositorio `checkcheck`. Sua tarefa e endurecer o formulario do modal `IA SETTINGS` para que ele nunca tente salvar configuracao sem um `project_id` valido e para que erros de catalogo de projetos nao sejam confundidos com erros de provider ou de chave. Trabalhe em `sistema/app/static/transport/app.js`, `sistema/app/static/transport/index.html` e, se necessario, em `sistema/app/static/transport/i18n.js`. Garanta que a dropdown tenha placeholder consistente, como `Selecione um projeto`, e que o botao `Save` permaneça desabilitado enquanto o catalogo estiver em estado `loading`, `empty`, `error` ou sem selecao valida. Mantenha `project_id` como valor canonico do formulario, nunca derivado de texto livre, nome do projeto ou estado legado. Reforce o tratamento local de erros HTTP 422, 404 e 409 para diferenciar claramente `projeto obrigatorio`, `projeto inexistente`, `projeto removido`, `catalogo indisponivel` e `provider sem chave valida`, evitando mensagens genericas como `Field required`. Se o contrato do modal tiver mudado de maneira incompatível para clientes antigos, atualize o versionamento dos assets do `transport` em `index.html` para reduzir risco de cache antigo no browser. Preserve as mensagens ja existentes que continuam corretas, nao quebre o prefixo relativo das rotas e mantenha a compatibilidade com a pagina servida sob prefixos como `/checking`. Valide no frontend que o `Save` fica bloqueado quando nao ha `project_id`, que a mensagem de erro correta aparece quando o backend reclama desse campo e que a interface nao confunde falha de catalogo com falha de provedor.

- [ ] Prompt 4 - Confirmar e endurecer a persistencia da chave por `project_id` no backend

  Voce e um agente de IA trabalhando no repositorio `checkcheck`. Seu objetivo nesta tarefa e confirmar, com teste e evidencia de codigo, que o modal `IA SETTINGS` grava configuracoes OpenAI por `project_id` em `transport_ai_project_llm_settings`, com criptografia, isolamento entre projetos e sem reaproveitamento indevido de segredos. Trabalhe principalmente em `sistema/app/routers/transport_ai.py`, `sistema/app/services/transport_ai_llm_settings.py`, `sistema/app/models.py` e nos testes relacionados. Verifique que `PUT /api/transport/ai/settings` recebe sempre `project_id`, `provider` e `api_key`, que o backend chama `upsert_transport_ai_llm_settings()` com o `project_id` esperado e que o dado persiste na tabela por projeto, e nao no singleton legado. Confirme que `api_key_ciphertext` e preenchido com valor diferente do segredo bruto, que `api_key_last4` e preenchido corretamente e que o `GET /api/transport/ai/settings?project_id=...` devolve apenas hint mascarado e metadados seguros. Se identificar qualquer ambiguidade nas respostas, refine as mensagens de erro 404, 409 e 422 para deixar claro se o projeto esta ausente, nao selecionado, removido ou sem configuracao. Preserve as regras de auditoria e rollback de sessao antes de registrar falhas, de forma que uma tentativa de save mal sucedida nao comite estado parcial. Nao redesenhe a arquitetura; foque em endurecer o comportamento ja existente. Valide com testes estreitos que projeto A e projeto B permanecem isolados, inclusive quando o provider muda e exige nova chave.

- [ ] Prompt 5 - Revalidar a leitura de runtime e os snapshots por projeto

  Voce e um agente de IA trabalhando no repositorio `checkcheck`. Sua tarefa e garantir que a configuracao salva para cada projeto seja a mesma configuracao efetivamente usada pelo runtime da IA e pelos snapshots de execucao. Trabalhe em `sistema/app/services/transport_ai_llm_settings.py`, `sistema/app/services/transport_ai_runtime.py`, `sistema/app/services/transport_ai_agent.py`, `sistema/app/services/transport_ai_runs.py` e `sistema/app/routers/transport_ai.py`. Confirme que o runtime resolve `project_id` por projeto ou particao da execucao, que nao ha compartilhamento acidental de segredo entre projetos e que conflitos entre configuracoes de runtime diferentes continuam falhando em preflight com mensagem clara. Verifique tambem que `llm_runtime_projects` continua refletindo o projeto correto, sem serializar `api_key` ou `api_key_ciphertext`, e que historico, diagnostico e latest suggestion nao mentem quando ha mais de um projeto na mesma run. Caso encontre divergencia entre o save e o consumo em runtime, aplique a menor correcao coerente no ponto de leitura, sem criar atalhos paralelos nem reintroduzir fallback global implicito. Depois da primeira mudanca substantiva, rode uma validacao focada no fluxo de runtime que possa falsificar a hipotese atual antes de seguir com outros arquivos.

- [ ] Prompt 6 - Permitir a alocacao de passageiros em veiculos ainda sem placa e cor

  Voce e um agente de IA trabalhando no repositorio `checkcheck`. Sua tarefa e corrigir o bloqueio que impede a alocacao de passageiros em um veiculo que ainda nao tenha informacoes de placa e cor, pois isso prejudica a capacidade da IA de distribuir passageiros durante o planejamento operacional. Nao assuma que a regra atual falha pelo mesmo caminho para os dois campos; primeiro confirme exatamente onde a alocacao esta sendo barrada e depois remova apenas as dependencias indevidas. Trabalhe principalmente em `sistema/app/services/transport_vehicle_base.py`, `sistema/app/services/transport.py`, `sistema/app/services/transport_assignment_operations.py`, `sistema/app/services/transport_proposals.py`, `sistema/app/services/transport_ai_planning.py` e `sistema/app/schemas.py`. Verifique como `build_transport_vehicle_pending_fields()` e `is_transport_vehicle_ready_for_allocation()` definem a prontidao do veiculo, como `is_ready_for_allocation` e `pending_fields` sao expostos nos schemas, como a alocacao manual e a alocacao da IA filtram ou rejeitam veiculos incompletos e se alguma parte do fluxo ainda usa `placa` como identidade obrigatoria em vez de `vehicle_id`, `vehicle_ref` ou outro identificador estavel. Implemente a menor correcao coerente para que placa e cor sejam tratadas como metadados opcionais ou campos pendentes durante a alocacao, sem impedir que o veiculo receba passageiros quando os requisitos realmente operacionais estiverem presentes, como tipo, capacidade, tolerancia, agenda e demais restricoes necessarias. Se algum trecho downstream ainda precisar exibir o veiculo sem placa, use um fallback controlado de apresentacao, sem inventar dados falsos nem gerar placas temporarias fora dos pontos em que isso for absolutamente obrigatorio e ja fizer parte de um fluxo explicito. Preserve validacoes que continuam necessarias para persistencia, exportacao, auditoria ou cadastro final, mas nao bloqueie a distribuicao de passageiros apenas porque o veiculo ainda nao foi totalmente identificado visualmente. Inclua testes focados que comprovem que a IA e o fluxo operacional conseguem alocar passageiros em veiculos sem placa e cor, e que essa flexibilizacao nao abre regressao em validacoes de capacidade, agenda ou integridade do cadastro.

- [ ] Prompt 7 - Ampliar e endurecer os testes automatizados de frontend para a dropdown e o modal

  Voce e um agente de IA trabalhando no repositorio `checkcheck`. Seu objetivo e ampliar os testes automatizados de frontend em `tests/transport_page_date.test.js` para cobrir integralmente o novo contrato do modal `IA SETTINGS` com dropdown de projetos autoritativa. Implemente casos que comprovem: populacao correta da dropdown a partir de `dashboard.projects` quando o dashboard traz itens validos; refresh autoritativo com `GET /projects` ao abrir o modal; recuperacao correta quando `dashboard.projects` vier vazio e `/projects` vier preenchido; estado controlado quando `/projects` vier vazio; erro especifico de catalogo quando `/projects` falhar; bloqueio do `Save` quando nao houver `projectId` valido; mensagem clara quando o backend retornar 422 por `project_id` ausente; isolamento de `provider`, `api_key_hint` e draft ao trocar de projeto; ordem correta do mock de `fetch` para que `/ai/settings` nao seja capturado pelo mock generico de `/settings`; e versionamento dos assets do `transport` quando houver mudanca incompatível no contrato do modal. Nao escreva testes frouxos ou apenas snapshots amplos; prefira asserts comportamentais que realmente falhem se a dropdown voltar a ficar vazia, se o save sair sem `project_id` ou se a UX voltar a mostrar erro enganoso. Se precisar ajustar o harness local para suportar novos estados do catalogo, faca isso de forma pequena e explicita. Ao final, rode somente o recorte de testes de frontend relevante e comprove que ele cobre a nova logica sem introduzir flakiness.

- [ ] Prompt 8 - Ampliar e endurecer os testes automatizados de backend para persistencia e sanitizacao

  Voce e um agente de IA trabalhando no repositorio `checkcheck`. Sua tarefa e ampliar os testes de backend para cobrir o fluxo de persistencia OpenAI por projeto, o isolamento entre projetos, as mensagens de erro e a sanitizacao de segredos. Trabalhe principalmente em `tests/test_transport_ai_llm_settings.py`, `tests/test_transport_ai_router.py`, `tests/test_transport_ai_runtime.py` e `tests/test_transport_ai_agent_runtime.py`. Escreva testes que verifiquem, de forma explicita, que um save OpenAI para projeto A persiste `provider=openai`, `api_key_ciphertext` preenchido e `api_key_last4` correto; que um save para projeto B nao altera A; que `GET /ai/settings?project_id=A` devolve apenas hint mascarado de A; que `PUT /ai/settings` sem `project_id` falha com erro controlado; que projeto inexistente retorna 404 apropriado; que troca de provider exige nova chave quando aplicavel; que falhas nao vazam segredo bruto nem ciphertext; e que runtime e snapshots continuam sem `api_key` nem `api_key_ciphertext`. Preserve a separacao entre testes de contrato e testes de runtime, e lembre-se de passar qualquer `settings_obj` explicito quando o teste precisar isolar configuracoes locais para nao repetir o erro de contaminar a execucao com o objeto global de settings. Execute apenas os testes tocados ou o recorte mais estreito que comprove o comportamento.

- [ ] Prompt 9 - Criar o smoke opt-in com OpenAI real sem gravar o segredo no repositorio

  Voce e um agente de IA trabalhando no repositorio `checkcheck`. Seu objetivo e criar um smoke test opt-in para o provider OpenAI real, sem depender da chave no CI padrao e sem gravar o segredo em arquivos do repositorio. O teste deve rodar somente quando uma variavel de ambiente como `TRANSPORT_AI_TEST_OPENAI_API_KEY` estiver presente. Escolha a menor superficie segura para implementar isso: pode ser um teste automatizado opt-in, um script de smoke controlado ou um comando de validacao claramente separado do fluxo padrao. O comportamento obrigatorio e: abrir um ambiente de preview ou teste isolado, autenticar no `/transport`, garantir que exista pelo menos um projeto de teste, salvar `provider=openai` para um projeto com a chave vinda apenas da variavel de ambiente, confirmar que o GET subsequente mostra `api_key_hint` mascarado, validar que existe linha criptografada para o `project_id` correto e executar o fluxo de IA ate pelo menos o estado `suggestion ready`, sem erro de configuracao. Em nenhum momento o segredo bruto pode aparecer em asserts, logs, markdown, fixtures, snapshots, mensagens de excecao ou artefatos versionados. Se a variavel de ambiente nao estiver presente, o teste deve ser pulado automaticamente. Documente no proprio teste ou script como executa-lo manualmente, mas nao vaze o segredo real e nao transforme esse smoke em dependencia obrigatoria do CI.

- [ ] Prompt 10 - Executar a validacao manual com a chave temporaria OpenAI e coletar evidencias

  Voce e um agente de IA trabalhando no repositorio `checkcheck`. Sua tarefa e executar, de forma manual e controlada, a validacao real do fluxo OpenAI por projeto usando a chave temporaria fornecida pelo operador, tratada apenas como segredo operacional e nunca como conteudo versionado. Antes de iniciar, garanta que `TRANSPORT_AI_SETTINGS_ENCRYPTION_KEY` esteja valida no ambiente, que a sessao `transport` esteja autenticada e que existam dois projetos de teste, um para o fluxo OpenAI e outro para controle negativo. Injete a chave apenas no terminal atual, prompt manual ou ambiente local, sem persisti-la em arquivo. Depois, abra o modal `IA SETTINGS`, confirme que a dropdown lista os dois projetos, selecione o projeto A, salve `provider=OpenAI` com a chave temporaria, confirme o `api_key_hint` mascarado no reload, consulte diretamente a tabela `transport_ai_project_llm_settings` para verificar `project_id`, `provider=openai`, `api_key_ciphertext` preenchido e ausencia de segredo bruto em texto claro, troque para o projeto B para confirmar que o hint nao foi herdado e execute o fluxo de IA para um request do projeto A. Revise logs e auditoria para garantir que nao houve vazamento do segredo. Ao final, remova a chave do ambiente e encerre a sessao ou preview usada no teste. Entregue um resumo objetivo das evidencias coletadas: payload HTTP sanitizado, resposta do GET com hint mascarado, linha de banco validando o projeto correto e status da run comprovando que o runtime leu a configuracao certa.

- [ ] Prompt 11 - Atualizar a documentacao operacional, verificar criterios de aceite e preparar rollback minimo

  Voce e um agente de IA trabalhando no repositorio `checkcheck`. Sua tarefa final e consolidar a rodada com documentacao operacional clara, conferencia dos criterios de aceite e registro do rollback minimo planejado. Atualize os documentos necessarios para deixar explicito que o catalogo do modal `IA SETTINGS` deve ser carregado do endpoint autoritativo `/api/transport/projects`, que `project_id` e obrigatorio no save, que chaves OpenAI reais so podem ser usadas em smoke opt-in fora do CI, que assets do `transport` devem ser versionados quando o contrato do frontend mudar de forma incompatível e que o caminho legado global continua apenas para consulta ou rollback controlado ate remocao futura. Depois disso, confronte a implementacao com todos os criterios de aceite do plano: dropdown nao vazia quando ha projetos e sessao valida, catalogo consistente e diagnosticavel, `Save` bloqueado sem `project_id`, persistencia apenas no projeto correto, ausencia de heranca de hint ou chave entre projetos, runtime coerente, possibilidade de alocar passageiros em veiculos ainda sem placa e cor quando os demais requisitos operacionais estiverem validos, testes automatizados cobrindo frontend e backend, smoke OpenAI opt-in executado sem vazamento e runbook suficiente para repeticao do processo. Se alguma parte ainda nao estiver concluida, nao esconda a lacuna: registre exatamente o que falta, qual evidencia ainda nao existe e qual e o menor proximo passo. Preserve um rollback minimo que reverta apenas a menor superficie necessaria sem reintroduzir chave global implicita nem destruir dados por projeto.

## Observacao final

Se estes prompts forem executados em sequencia, a recomendacao e respeitar a ordem acima. Ela foi montada para evitar que uma falha de interface esconda um problema de persistencia, ou que uma lacuna de testes esconda uma regressao de runtime.