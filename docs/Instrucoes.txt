Rodar a API (porta padrao 8000):
.\.venv\Scripts\python.exe -m uvicorn sistema.app.main:app --host 0.0.0.0 --port 8000

Rodar a API sem conflito de portas:
.\.venv\Scripts\python.exe -m uvicorn sistema.app.main:app --host 0.0.0.0 --port 8001

Compilar firmware ESP32-S3:
arduino-cli compile --fqbn esp32:esp32:esp32s3 .\firmware\esp32_checking

Enviar firmware para COM5:
arduino-cli upload -p COM5 --fqbn esp32:esp32:esp32s3 .\firmware\esp32_checking

Instalar dependencias de firmware no Arduino CLI:
arduino-cli lib install "MFRC522"

Fluxo com 2 leitores RC522:
1. O leitor RC522 #1 envia `action=checkin` para /api/scan.
2. O leitor RC522 #2 envia `action=checkout` para /api/scan.
3. A API executa a acao recebida e atualiza `users.checkin` apenas quando o envio ao Forms for bem-sucedido.
4. RFID desconhecido entra em pendencia na aba Cadastro.

Fluxo de boot e monitoramento da nuvem:
1. Ao energizar, o LED interno da ESP32 acende amarelo imediatamente.
2. A ESP32 tenta conectar ao Wi-Fi e enviar heartbeat para a API.
3. Quando recebe resposta positiva da API, o LED branco passa a piscar 1 vez por segundo.
4. A ESP32 repete o heartbeat a cada 3 minutos.
5. Em falha de conectividade ou resposta negativa, o LED vermelho fica aceso continuamente.
6. Enquanto o LED vermelho estiver aceso por indisponibilidade, a leitura de cartoes fica bloqueada.
7. Se a indisponibilidade permanecer por 30 segundos, a ESP32 reinicia para repetir a tentativa de conexao ao Wi-Fi e ao sistema.

Ligacao SPI dos dois RC522:
1. ESP32 GPIO 12 -> SCK dos dois RC522.
2. ESP32 GPIO 11 -> MOSI dos dois RC522.
3. ESP32 GPIO 13 -> MISO dos dois RC522.
4. ESP32 GPIO 9 -> RST dos dois RC522.
5. ESP32 GPIO 10 -> SDA/SS do RC522 #1.
6. ESP32 GPIO 14 -> SDA/SS do RC522 #2.
7. 3V3 -> VCC e GND comum para ambos.

Arquivo de referencia eletrica:
docs\esquematico_esp32_rc522_duplo.md

Observacao sobre heartbeat:
1. Corrigido no firmware para nao marcar falha quando a API retornar HTTP 200 com JSON compacto ("ok":true).

Monitoramento em tempo real:
1. Serial da ESP32 (diagnostico RC522 + Wi-Fi):
	arduino-cli monitor -p COM5 -c baudrate=115200

2. Estado da API:
	.\.venv\Scripts\python.exe -c "import urllib.request; print(urllib.request.urlopen('http://172.20.10.3:8000/api/health').read().decode())"

3. Contadores do banco (heartbeats, eventos, pendencias):
	.\.venv\Scripts\python.exe -c "import sqlite3; c=sqlite3.connect('checking.db'); cur=c.cursor(); print('heartbeats',cur.execute('select count(*) from device_heartbeats').fetchone()[0]); print('events',cur.execute('select count(*) from check_events').fetchone()[0]); print('pending',cur.execute('select count(*) from pending_registrations').fetchone()[0]); c.close()"